spring.application.name=demo

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/dali_certificates?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
logging.level.org.springframework.web=DEBUG

# Mail (non-sensitive)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Import secrets
spring.config.import=optional:application-secret.properties

# Respect X-Forwarded-* from ngrok (scheme/host)
server.forward-headers-strategy=framework

# ---- App URLs (update when ngrok rotates) ----
# Backend ngrok (8080) used in email links
app.backend-base=https://11c79ff5e909.ngrok-free.app

# Frontend ngrok (Angular 4200) used in redirect after verification
app.frontend-base=https://c6c21ca3441c.ngrok-free.app
