spring.application.name=demo

# MySQL Database Connection (non-sensitive)
spring.datasource.url=jdbc:mysql://localhost:3306/dali_certificates?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Debug Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
logging.level.org.springframework.web=DEBUG

# Mail Server (non-sensitive)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Import the secret config file
spring.config.import=optional:application-secret.properties

# Public URLs (from ngrok) — KEEP UPDATED if tunnels change
app.backend-base=https://fe050f4ceb46.ngrok-free.app
app.frontend-base=https://a6ee911ef883.ngrok-free.app

